using System;
namespase first_program {
	class Program {
		static void Main(string[] args) {
			const float score = 2.56f; // переменные которые не меняют свое значение в дальнейшем
			num += 1; // Принимает все основные математические операции
			num ++; // Умножить на один 
		}
	}
}